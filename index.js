// Create state 

const increment = "INCREMENT"
const decrement = "DECREMENT"
const reset = "RESET"

const initialState = {
    counter : 0
}

const incrementAction = {
    type : increment
}

const decrementAction = {
    type : decrement
}

const resetAction = {
    type : reset
}